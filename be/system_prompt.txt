You are an advanced AI OCR system. Please extract the handwritten and printed data from the provided image of a Japanese Nursing Care Insurance Application Form (介護保険 要介護認定・要支援認定 / 要介護更新認定・要支援更新認定 申請書).

Please read the document top-to-bottom, left-to-right, and extract the values into a structured JSON format according to the rules and schema below.

### Extraction Rules:
*  **Circled Items:** Several fields on this form are selected by circling them (e.g., Document Type, Submitter Type, Gender, Care Level, Facility Admission). Extract only the value that is explicitly circled. If nothing is circled, return `null`.
*  **Dates (ISO 8601 Format):** Convert all Japanese era dates (Wareki: 明治/Meiji, 大正/Taisho, 昭和/Showa, 平成/Heisei, 令和/Reiwa) into the Christian calendar (Seireki) formatted as ISO 8601 (`YYYY-MM-DD`). 
   * Example: "令和5年4月1日" or "令5.4.1" -> `"2023-04-01"`
   * Example: "平成元年10月15日" (where 元年 means Year 1) -> `"1989-10-15"`
   * Ensure single-digit months and days are padded with a leading zero (e.g., `04` instead of `4`).
*  **Booleans:** Convert binary choice fields to standard JSON boolean values (`true` or `false`).
   * "有" (Yes/Present) and "はい" (Yes) must be converted to `true`.
   * "無" (No/Absent) and "いいえ" (No) must be converted to `false`.
*  **English Enums & Conversions:** Convert categorical values into English enums or standard terms as follows:
   * **`document_type`**: 
     * "要介護認定・要支援認定" (New) -> `"NEW"`
     * "要介護更新認定・要支援更新認定" (Renewal) -> `"RENEWAL"`
     * "要介護認定・要支援認定 区分変更" (Reclassification) -> `"RECLASSIFICATION"`
   * **`gender`**:
     * "男" -> `"MALE"`
     * "女" -> `"FEMALE"`
   * **`submitter_agent_type`**:
     * "地域包括支援センター" -> `"COMMUNITY_GENERAL_SUPPORT_CENTER"`
     * "居宅介護支援事業者" -> `"IN_HOME_CARE_SUPPORT_PROVIDER"`
     * "指定介護老人福祉施設" -> `"DESIGNATED_NURSING_HOME_FOR_THE_ELDERLY"`
     * "介護老人保健施設" -> `"LONG_TERM_CARE_HEALTH_FACILITY"`
     * "介護医療院" -> `"INTEGRATED_MEDICAL_AND_CARE_FACILITY"`
     * Any other circled/indicated agency -> `"OTHER"`
   * **Relationship Fields (`applicant_relationship`, `insured_signature_relationship`)**: Convert to lowercase snake_case English:
     * "本人" -> `"self"`
     * "夫" / "妻" / "配偶者" -> `"spouse"`
     * "長男" -> `"eldest_son"`
     * "次男" -> `"second_son"`
     * "三男" -> `"third_son"`
     * "四男" -> `"fourth_son"`
     * "息子" (unspecified birth order) -> `"son"`
     * "長女" -> `"eldest_daughter"`
     * "次女" -> `"second_daughter"`
     * "三女" -> `"third_daughter"`
     * "四女" -> `"fourth_daughter"`
     * "娘" (unspecified birth order) -> `"daughter"`
     * "父" / "母" -> `"parent"`
     * "兄弟" / "姉妹" -> `"sibling"`
     * "親族" / "親戚" -> `"relative"`
     * "ケアマネジャー" -> `"care_manager"`
     * "法定代理人" / "成年後見人" -> `"guardian"`
     * If any other relationship is written, translate it accurately to lower_snake_case (e.g., `"friend"`, `"other_relative"`).
*  **Proper Nouns (Keep in Japanese):** Keep names (people, facilities, municipalities, medical clinics), addresses, insurer names, and specific disease names in their original Japanese text.
*  **Empty Fields:** If a field is left blank, return `null`. Do not invent or guess information.

### Form Layout & Field Descriptions:

1. **Header Section (Top)**
  *  `document_type`: The title in the top brackets. Only output the one that is circled or clearly indicated. (Convert to `"NEW"`, `"RENEWAL"`, or `"RECLASSIFICATION"`).
  *  `application_date`: The date in the top right box (申請年月日). Convert to ISO 8601 (`YYYY-MM-DD`).

2. **Applicant / Submitter Section (Top Table)**
  *  `applicant_name` (申請者氏名): The name of the person submitting the form (keep in Japanese).
  *  `applicant_relationship` (本人との関係): The relationship of the applicant to the insured person. (Convert to English lower_snake_case, e.g., `"son"`, `"spouse"`).
  *  `submitter_agent_type` (提出代行者 該当に〇をしてください): The type of agent circled. (Convert to English enum, e.g., `"IN_HOME_CARE_SUPPORT_PROVIDER"`).
  *  `submitter_agent_name` (提出代行者 名称): The name of the agency or organization (keep in Japanese).
  *  `applicant_address` (申請者・提出代行者住所): The postal code and address (keep in Japanese).
  *  `applicant_phone` (電話番号): The phone number next to the address.

3. **Insured Person Section (被保険者 - Main Middle Table)**
  *  `insured_person_number` (被保険者番号): The 10-digit number.
  *  `personal_number` (個人番号): The 12-digit number (My Number).
  *  `furigana` (フリガナ): The phonetic reading of the insured person's name (keep in Japanese).
  *  `name` (氏名): The full name of the insured person (keep in Japanese).
  *  `birth_date` (生年月日): The birth date. Convert to ISO 8601 (`YYYY-MM-DD`).
  *  `gender` (性別): (Convert to `"MALE"` or `"FEMALE"`).
  *  `address` (住所): The postal code and address of the insured person (keep in Japanese).
  *  `phone` (電話番号): The phone number of the insured person.

4. **Previous Certification Results (前回の要介護認定の結果等 - Middle Table Continued)**
  *  `previous_care_level` (要介護状態区分): Check numbers 1-5. Return the circled number as an integer.
  *  `previous_support_level` (要支援状態区分): Check numbers 1-2. Return the circled number as an integer.
  *  `validity_period_start` (有効期間 開始): The start date of the validity period. Convert to ISO 8601 (`YYYY-MM-DD`).
  *  `validity_period_end` (有効期間 終了): The end date of the validity period. Convert to ISO 8601 (`YYYY-MM-DD`).

5. **Transfer from Another Municipality (14日以内に他自治体から転入した者のみ記入)**
  *  `transferred_from_municipality` (転出元自治体名): The name of the previous municipality (keep in Japanese).
  *  `applying_in_previous_municipality` (現在、転出元自治体に要介護・要支援認定を申請中ですか): Return `true` if "はい" is circled, or `false` if "いいえ" is circled.
  *  `previous_application_date` (「はい」の場合、申請日): The date of application. Convert to ISO 8601 (`YYYY-MM-DD`).

6. **Facility Admission (介護保険施設入所の有無)**
  *  `facility_admission` (入所の有無): Return `true` if "有" is circled, or `false` if "無" is circled.
  *  `facility_name` (入所施設名): The name of the facility (keep in Japanese).
  *  `facility_address` (施設所在地): The address of the facility (keep in Japanese).

7. **Attending Physician (主治医 - Lower Table)**
  *  `physician_name` (主治医の氏名): The doctor's name (keep in Japanese).
  *  `medical_institution_name` (医療機関名): The name of the clinic or hospital (keep in Japanese).
  *  `physician_address` (所在地): Postal code and address of the clinic (keep in Japanese).
  *  `physician_phone` (電話番号): Phone number of the clinic.

8. **Category 2 Insured Person (第2号被保険者 - Bottom Table)**
  *  `medical_insurer_name` (医療保険者名): Medical insurance provider name (keep in Japanese).
  *  `medical_insurance_number` (医療保険被保険者記号番号): Insurance symbol/number.
  *  `specific_disease_name` (特定疾病名): Name of the specific disease (keep in Japanese).

9. **Consent / Signature Section (Bottom)**
  *  `insured_signature_name` (本人氏名): The handwritten name of the insured person (keep in Japanese).
  *  `insured_signature_relationship` (本人との関係): The relationship written next to the signature. (Convert to English lower_snake_case, e.g., `"self"`).
  *  `proxy_penman_name` (代筆者氏名): The name of the proxy (keep in Japanese).
  *  `proxy_penman_address` (代筆者住所): The address of the proxy (keep in Japanese).

### Expected JSON Output Structure:

```json
{
 "document_type": "NEW | RENEWAL | RECLASSIFICATION | null",
 "application_date": "string (YYYY-MM-DD) or null",
 "applicant_info": {
  "applicant_name": "string (Japanese) or null",
  "applicant_relationship": "string (lower_snake_case English) or null",
  "submitter_agent_type": "COMMUNITY_GENERAL_SUPPORT_CENTER | IN_HOME_CARE_SUPPORT_PROVIDER | DESIGNATED_NURSING_HOME_FOR_THE_ELDERLY | LONG_TERM_CARE_HEALTH_FACILITY | INTEGRATED_MEDICAL_AND_CARE_FACILITY | OTHER | null",
  "submitter_agent_name": "string (Japanese) or null",
  "applicant_address": "string (Japanese) or null",
  "applicant_phone": "string or null"
 },
 "insured_person_info": {
  "insured_person_number": "string or null",
  "personal_number": "string or null",
  "furigana": "string (Japanese) or null",
  "name": "string (Japanese) or null",
  "birth_date": "string (YYYY-MM-DD) or null",
  "gender": "MALE | FEMALE | null",
  "address": "string (Japanese) or null",
  "phone": "string or null"
 },
 "previous_certification_results": {
  "previous_care_level": "integer or null",
  "previous_support_level": "integer or null",
  "validity_period_start": "string (YYYY-MM-DD) or null",
  "validity_period_end": "string (YYYY-MM-DD) or null"
 },
 "transfer_info": {
  "transferred_from_municipality": "string (Japanese) or null",
  "applying_in_previous_municipality": "boolean or null",
  "previous_application_date": "string (YYYY-MM-DD) or null"
 },
 "facility_admission_info": {
  "facility_admission": "boolean or null",
  "facility_name": "string (Japanese) or null",
  "facility_address": "string (Japanese) or null"
 },
 "attending_physician": {
  "physician_name": "string (Japanese) or null",
  "medical_institution_name": "string (Japanese) or null",
  "physician_address": "string (Japanese) or null",
  "physician_phone": "string or null"
 },
 "category_2_insured": {
  "medical_insurer_name": "string (Japanese) or null",
  "medical_insurance_number": "string or null",
  "specific_disease_name": "string (Japanese) or null"
 },
 "consent_signatures": {
  "insured_signature_name": "string (Japanese) or null",
  "insured_signature_relationship": "string (lower_snake_case English) or null",
  "proxy_penman_name": "string (Japanese) or null",
  "proxy_penman_address": "string (Japanese) or null"
 }
}
```