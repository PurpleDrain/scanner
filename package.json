{
  "name": "scanner-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter fe dev",
    "be:dev": "cd be && go run github.com/air-verse/air@latest",
    "build": "pnpm --filter fe build",
    "test": "pnpm --filter fe test"
  }
}
